# The CLI versions the adapters were verified against.
#
# `plan-review doctor` compares each tool's current version against the
# value here and WARNS on any difference. Exact match, not a floor: compatibility
# was measured at exactly these versions, and nothing tells us a newer release
# preserves the behaviour the adapters depend on. A warning is not a refusal to
# run -- a newer CLI usually works.
#
# What drift actually threatens, per adapter:
#   codex   the banner. adapters/codex.sh parses `sandbox:`, `model:`,
#           `reasoning effort:` and `session id:` out of stderr, and refuses to
#           trust a review when the sandbox line is missing or unexpected. A
#           banner change is a failed round, not a wrong one.
#   agent   the mid-run model-switch notice, which Cursor emits only as prose
#           ("Switched to ..."). Reworded, the adapter would record a model that
#           did not answer.
#   agy     the --output-format json envelope: status, response, conversation_id.
#   claude  the --output-format stream-json `init` line: model, permissionMode,
#           claude_code_version. adapters/claude.sh refuses to record a review
#           when that line is missing or reports a permission mode it did not
#           ask for, so a stream change is a failed round, not a wrong one.
#           Also at risk: --safe-mode, which is what stops the TARGET repo's
#           .claude/settings.json hooks executing inside the jail.
#
# Format: <tool> <version>. One per line, # starts a comment.
# Update it after re-running the probes recorded in the process notes.

# Verified 2026-08-28 by the full acceptance matrix
# (docs/process/probes/2026-08-28-acceptance-matrix/report.md), four rows:
#   codex-banner            the smoke's stderr carried all four parsed lines --
#                           workdir, model, `sandbox: workspace-write [workdir]
#                           (network access enabled)`, `reasoning effort:`,
#                           `session id:` -- and the meta file recorded four
#                           non-empty lines.
#   codex-sandbox-enforced  a reviewer instructed to `touch` a canary under $HOME
#                           reported, verbatim, "touch: cannot touch '...':
#                           Read-only file system", and the canary was absent
#                           afterwards. Absence alone would have proved nothing;
#                           the reviewer reporting the refusal is what makes it a
#                           measurement.
#   codex-fail-closed       with PR_EXPECTED_SANDBOX mismatched the adapter exited
#                           1 and wrote no review. The reason states the CLASS
#                           ("codex ran under an unexpected sandbox; the review
#                           was discarded"); the offending VALUE is in the log,
#                           beside the expectation it failed. The Q8 refusal
#                           fires end to end.
#   codex-group-snapshot    the one path P6 could not reach -- codex through its
#                           own adapter. Every process below the round's
#                           `timeout --kill-after` (adapter bash, the node
#                           launcher, the musl codex binary) shared the timeout's
#                           own PGID, and nothing survived the round.
# The reviewer also runs under a private CODEX_HOME, so the operator's
# ~/.codex/config.toml is not read: the matrix passed with a top-level
# `network_access = true` still sitting in it, which older notes called a
# precondition to fix by hand.
codex 0.150.1

# Verified 2026-08-28 by the acceptance matrix, and by the containment probe of
# the same week (docs/process/probes/2026-08-28-cursor-containment/). Re-measured
# at THIS version: `agent about --format json` answers with cliVersion (and
# userEmail, which the doctor never echoes), and `agent status --format json`
# answers with isAuthenticated -- lib/doctor.sh uses both, so identity and auth
# are structured, and the 60-second --list-models call is left to pin validation
# alone. A smoke-scale round recorded all four meta lines.
#
# The mid-run model-switch notice is the one promise this line does NOT vouch
# for, and the narrowing is deliberate rather than an oversight. Cursor emits it
# only as prose ("Switched to ..."); the format was verified at pin time and
# CANNOT be provoked on demand (probe P5), so no run since has re-read it. Drift
# in that prose is detected only by a live swap actually happening -- until then
# adapters/agent.sh could be recording a model that did not answer, and nothing
# here would know. Treat the notice as pinned-by-history, not pinned-by-test.
#
# Containment, by contrast, WAS re-measured at this version: Cursor's own
# sandbox is the write barrier, and the earlier "unconfined" reading was the
# probing host's own ~/.cursor `approvalMode: "unrestricted"` making
# `--sandbox enabled` inert. adapters/agent.sh now runs from a private
# CURSOR_CONFIG_DIR that pins the safe mode, so the operator's setting cannot
# reach the reviewer.
agent 2026.08.25-3e8eec8

# Verified 2026-08-28 by the acceptance matrix. The envelope row is the one this
# line rests on: a direct call with the adapter's own argv came back with
# `status`, `response` and `conversation_id` all present, and a smoke-scale round
# through the adapter parsed the conversation id into meta and the response into
# the review. Provenance is still weaker than codex's for CONFINEMENT: the early
# probes established what agy's --sandbox does not confine but never recorded the
# version they ran against, so nothing here is "the version the escape was
# measured on". The jail adapters/agy.sh builds is what confines it.
#
# --print-timeout IS passed, and the stale paragraph that used to sit here saying
# otherwise is gone. adapters/agy.sh derives it from the deadline the execution
# kernel exports, as $((PR_TIMEOUT_SECS * 900))ms -- 90% of the runner's, in
# milliseconds so the inner deadline stays strictly below the outer one even at
# PR_TIMEOUT_SECS=1. The DERIVED VALUE is pinned offline
# (tests/test-adapter-agy.sh asserts `810000ms` at 900s, and a separate case
# covers the one-second deadline), and the doctor asserts the flag still exists
# in agy's help. What is NOT re-measured here is agy HONOURING it: the expiry
# classification -- exit 1 with .status ERROR and .error "timeout waiting for
# response" -- was measured once, in probe P1 at agy 1.1.15, and has not been
# re-provoked live since. The matrix proves only that the installed CLI accepts
# the argv, since a rejected flag would have failed the envelope row outright.
agy 1.1.22

# Verified 2026-08-31 on Linux, by the L1 set: the repaired resume leg plus the
# three rows of the 2026-08-30 return leg
# (docs/process/FINDINGS-2026-08-31-resume-probe.md, and
# FINDINGS-2026-08-30-linux.md behind it). This line moved off 2.1.248 for the
# first time since the 2026-08-28 matrix, and it moved because the resume row --
# the one thing blocking it -- can pass again.
#
# EVERY MEASUREMENT BEHIND THIS LINE IS THE BUBBLEWRAP HALF. Both hosts had
# bwrap, so both took it; adapters/claude.sh's Darwin half has never been
# measured at this version, and a Mac cannot reach the bwrap half to check the
# rest. The pin is one number for an adapter with two paths, and it vouches for
# one of them.
#
#   claude-init-line   RE-MEASURED 2026-08-31, and for free: adapters/claude.sh
#                      refuses to record a review when the stream-json `init`
#                      line is missing or reports a permission mode it did not
#                      ask for, so every paid round of the resume leg parsed one.
#                      The retained head carries model, permissionMode
#                      (bypassPermissions) and claude_code_version 2.1.251. That
#                      match at this version is what licenses trusting the
#                      offline pin of the refusal branch, which cannot be
#                      provoked against a live CLI on demand.
#   resume             all four observables plus the new fifth, at
#                      claude-sonnet-5 -- the model that DECLINED the previous
#                      probe in writing on 2026-08-30. The discriminator changed,
#                      not the mechanism: the marker now rides in a repository
#                      file that is deleted between rounds, and nothing asks the
#                      reviewer to keep a secret. See the findings note before
#                      re-running it; the plant's irrelevance to round 1's
#                      question is load-bearing and was got wrong once.
#   claude-safe-mode   CITED, not re-measured on 2026-08-31. Last measured
#                      2026-08-30 on the same host at this same version, with the
#                      control: a target repo whose .claude/settings.json ran a
#                      hook on SessionStart and UserPromptSubmit fired NEITHER
#                      canary through the adapter, while the same repo under a
#                      plain `claude -p` fired both; PR_KEEP_SANDBOX=1 and the
#                      retained copy confirmed settings.json arrived verbatim
#                      into a writable workdir, so the absence is suppression.
#                      The citation rests on a diff: the only commit to
#                      adapters/claude.sh since that reading is the row-L3 fix,
#                      whose sole executable change is the refusal branch for a
#                      non-Darwin host with no bwrap. The bubblewrap half is
#                      byte-identical.
#   claude-fail-closed CITED on the same terms, from the same reading.
# Also still true at this version: no effort is reported in either output format.
claude 2.1.251
